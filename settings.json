{
  "model": {
    "name": "tiny",
    "options": ["tiny", "base", "small", "medium", "large", "turbo"],
    "description": "Whisper model to use for transcription (tiny=fastest, turbo=most accurate and fast, but needs more memory)"
  },
  "audio": {
    "sample_rate": 44100,
    "channels": 2,
    "sample_format": "int16"
  },
  "output": {
    "mode": "type",
    "options": ["type", "paste", "clipboard"],
    "description": "type=character by character, paste=immediate paste, clipboard=copy only"
  },
  "type": {
    "delay": 0.005,
    "description": "Delay in ms between single key pressed in the type output mode. Increase this value if you miss letters."
  }
}
